# Email to Email AI Agent Configuration
# Update with your Gmail credentials and preferences

email:
  email: "yashtech1920@gmail.com"  # Your Gmail address
  app_password: "hlsr jmqg anhd fwcw"  # Gmail App Password (not regular password)
  imap_server: "imap.gmail.com"
  smtp_server: "smtp.gmail.com"
  imap_port: 993
  smtp_port: 587

agent:
  auto_reply: true          # Enable automatic replies
  reply_delay: 5           # Wait 5 seconds before sending reply (more natural)
  check_interval: 30       # Check for new emails every 30 seconds
  max_emails_per_check: 5  # Process max 5 emails per check

ai:
  enabled: true              # Set to true to use Gemini (requires API key)
  model: "gemini-pro"        # Google Gemini model to use
  tone: "professional"       # professional, friendly, brief
  api_key: "AIzaSyA1lkemx839WTCMxLd6D10T7rwiwvsmGVc"  # Your Gemini API key